{
  "verbose": true,
  "ignore": ["*.test.js", "src/**/*", "dist/**/*"],
  "env": {
    "NODE_ENV": "dev"
  },
  "ext": "js json",
  "events": {
    "restart": "osascript -e 'display notification \"app restarted\" with title \"camperdays\"'",
    "crash": "osascript -e 'display notification \"app crashed\" with title \"camperdays\"'"
  }
}
